---
phase: phase-6
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - webui/package.json
  - webui/tsconfig.json
  - webui/tailwind.config.js
  - webui/postcss.config.js
  - webui/src/main.tsx
  - webui/src/App.tsx
  - webui/src/index.css
  - webui/src/components/StockCard.tsx
  - webui/src/components/RadarChart.tsx
  - webui/src/components/LogicSummary.tsx
  - webui/src/components/MacroOverview.tsx
  - webui/src/hooks/useRecommendations.ts
  - webui/src/services/api.ts
autonomous: true
requirements:
  - WEB-02

must_haves:
  truths:
    - "User can see stock recommendation cards with key data (price, change, position, marker)"
    - "User can view radar charts showing logic/market/macro scores"
    - "User can see logic affiliation summaries for each stock"
    - "User can view macro overview dashboard with regime status"
  artifacts:
    - path: "webui/src/components/StockCard.tsx"
      provides: "Stock recommendation card component"
      exports: ["StockCard"]
    - path: "webui/src/components/RadarChart.tsx"
      provides: "Recharts-based radar visualization"
      exports: ["StockRadarChart"]
    - path: "webui/src/components/LogicSummary.tsx"
      provides: "Logic affiliation display component"
      exports: ["LogicSummary"]
    - path: "webui/src/components/MacroOverview.tsx"
      provides: "Macro regime dashboard component"
      exports: ["MacroOverview"]
    - path: "webui/src/hooks/useRecommendations.ts"
      provides: "TanStack Query hook for recommendations"
      exports: ["useRecommendations"]
    - path: "webui/src/services/api.ts"
      provides: "API client and query keys"
      exports: ["queryKeys", "api"]
  key_links:
    - from: "webui/src/components/StockCard.tsx"
      to: "/api/v1/recommendations"
      via: "useRecommendations hook"
      pattern: "useQuery.*recommendations"
    - from: "webui/src/components/RadarChart.tsx"
      to: "recharts"
      via: "RadarChart component import"
      pattern: "from ['\"]recharts['\"]"
    - from: "webui/src/hooks/useRecommendations.ts"
      to: "webui/src/services/api.ts"
      via: "fetch function"
      pattern: "fetch.*api/v1"
---

<objective>
Build React frontend with Vite + TypeScript, displaying stock cards, radar charts, logic summaries, and macro overview dashboard.

Purpose: Provide user-facing dashboard for stock recommendations and visualizations per WEB-02 requirement.
Output: Working React SPA with four core components, TanStack Query state management, and TailwindCSS styling.
</objective>

<execution_context>
@/Users/heyecheng/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heyecheng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-6/06-RESEARCH.md
</context>

<interfaces>
<!-- Key types from RESEARCH.md - frontend will consume these API shapes -->

From 06-RESEARCH.md - API Response Shapes:
```typescript
// GET /api/v1/recommendations response
interface RecommendationResponse {
  stock_code: string;
  stock_name: string | null;
  current_price: number | null;
  change_percent: number | null;
  recommended_position: number;
  position_tier: string;
  marker: string;  // '逻辑受益股' | '关联受益股' | '情绪跟风股'
  marker_reason: string | null;
  logic_score: number;
  market_score: number;
  composite_score: number;
}

// Radar chart data shape
interface RadarDataPoint {
  subject: string;
  value: number;
  fullMark: number;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite + React + TypeScript project with dependencies</name>
  <files>webui/package.json, webui/tsconfig.json, webui/vite.config.ts</files>
  <action>
    Create webui/ directory structure:
    1. Run: npm create vite@latest webui -- --template react-ts
    2. Install core dependencies:
       - @tanstack/react-query@5.99.2 (server state)
       - recharts@3.8.1 (radar charts)
       - react-router-dom (navigation)
    3. Install dev dependencies:
       - tailwindcss, postcss, autoprefixer
       - @types/node
    
    Configure tsconfig.json with:
    - strict: true
    - moduleResolution: "bundler"
    - paths: { "@/*": ["./src/*"] }
    
    Configure vite.config.ts with:
    - Proxy /api/v1 to http://localhost:8000 (backend)
    - Alias @ to src directory
  </action>
  <verify>
    <automated>cd webui && npm install && npm run build</automated>
  </verify>
  <done>
    Vite project created; all dependencies installed; build succeeds without TypeScript errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure TailwindCSS and create base styles</name>
  <files>webui/tailwind.config.js, webui/postcss.config.js, webui/src/index.css</files>
  <action>
    1. Run: npx tailwindcss init -p in webui/
    
    2. Configure tailwind.config.js:
       - content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"]
       - theme.extend.colors for stock-specific colors:
         - stock-up: #16a34a (green for gains in A-share context)
         - stock-down: #dc2626 (red for losses)
         - stock-neutral: #6b7280
    
    3. Create src/index.css with:
       - @tailwind base, components, utilities
       - Base styles: font-family, background, text color
       - Utility classes for stock cards (.stock-card, .stock-card-hover)
    
    4. Update src/main.tsx to import index.css
  </action>
  <verify>
    <automated>cd webui && npm run build</automated>
  </verify>
  <done>
    TailwindCSS configured; custom stock colors defined; build succeeds; index.css imported in main.tsx.
  </done>
</task>

<task type="auto">
  <name>Task 3: Set up TanStack Query and API service layer</name>
  <files>webui/src/services/api.ts, webui/src/hooks/useRecommendations.ts, webui/src/App.tsx</files>
  <action>
    1. Create webui/src/services/api.ts:
       - Base URL: /api/v1 (proxied via Vite)
       - Export queryKeys object: { recommendations: ['recommendations'], stock: (code) => ['stock', code], macro: ['macro'] }
       - Export api object with fetch functions:
         - getRecommendations(): Promise<RecommendationResponse[]>
         - getStockDetail(code: string): Promise<StockDetailResponse>
         - getMacroOverview(): Promise<MacroResponse>
    
    2. Create webui/src/hooks/useRecommendations.ts:
       - useQuery with queryKey: queryKeys.recommendations
       - staleTime: 5 * 60 * 1000 (5 minutes)
       - Returns: { data, isLoading, error }
    
    3. Update webui/src/App.tsx:
       - Wrap with QueryClientProvider
       - Configure QueryClient with defaultOptions (staleTime: 5 min)
       - Add BrowserRouter for routing
  </action>
  <verify>
    <automated>cd webui && npm run build</automated>
  </verify>
  <done>
    API service exports fetch functions; useRecommendations hook created; App.tsx wrapped with providers.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create StockCard component for recommendation display</name>
  <files>webui/src/components/StockCard.tsx</files>
  <action>
    Create webui/src/components/StockCard.tsx:
    
    Props interface:
    - stock: RecommendationResponse
    
    Component structure:
    1. Card container with border, rounded-lg, p-4, hover:shadow-md
    2. Header: stock_name + stock_code, marker badge (color-coded)
    3. Price section: current_price, change_percent (green/red color)
    4. Position section: recommended_position %, position_tier
    
    Marker colors (from RESEARCH.md):
    - '逻辑受益股': bg-green-100 text-green-800
    - '关联受益股': bg-blue-100 text-blue-800
    - '情绪跟风股': bg-yellow-100 text-yellow-800
    
    Export as named export: StockCard
  </action>
  <verify>
    <automated>cd webui && npm run build</automated>
  </verify>
  <done>
    StockCard component renders stock data; marker badge shows correct colors; TypeScript types are correct.
  </done>
</task>

<task type="auto">
  <name>Task 5: Create RadarChart component for score visualization</name>
  <files>webui/src/components/RadarChart.tsx</files>
  <action>
    Create webui/src/components/RadarChart.tsx:
    
    Imports from recharts:
    - RadarChart, PolarGrid, PolarAngleAxis, PolarRadiusAxis, Radar, Legend, Tooltip, ResponsiveContainer
    
    Props interface:
    - data: RadarDataPoint[]
    - width?: number (default 400)
    - height?: number (default 300)
    
    Component structure:
    1. ResponsiveContainer wrapping RadarChart
    2. PolarGrid, PolarAngleAxis (dataKey="subject")
    3. PolarRadiusAxis with domain [0, 1]
    4. Radar with dataKey="value", stroke + fill colors, fillOpacity=0.6
    5. Legend and Tooltip
    
    Default radar dimensions (from RESEARCH.md pattern):
    - Logic Score, Market Score, Macro Fit, Sector State, Sentiment
  </action>
  <verify>
    <automated>cd webui && npm run build</automated>
  </verify>
  <done>
    RadarChart renders with Recharts; accepts data prop; ResponsiveContainer works; no TypeScript errors.
  </done>
</task>

<task type="auto">
  <name>Task 6: Create LogicSummary and MacroOverview components</name>
  <files>webui/src/components/LogicSummary.tsx, webui/src/components/MacroOverview.tsx</files>
  <action>
    1. Create webui/src/components/LogicSummary.tsx:
       - Props: { logicId: string; logicName: string; direction: string; strength: number; family: string }
       - Display: logic name, family badge, direction indicator (up/down arrow), strength bar
       - Strength bar: visual progress bar (0-100%)
    
    2. Create webui/src/components/MacroOverview.tsx:
       - Props: { regime: string; quadrant: string; multiplier: number; scores: Record<string, number> }
       - Display: regime status card, monetary-credit quadrant, multiplier badge
       - Five dimension scores as mini progress bars:
         - Liquidity, Growth, Inflation/Cost, Policy, Global
       - Color coding: high (>0.7) green, medium (0.4-0.7) yellow, low (<0.4) red
  </action>
  <verify>
    <automated>cd webui && npm run build</automated>
  </verify>
  <done>
    LogicSummary shows logic details with strength bar; MacroOverview displays regime and five dimensions; both build without errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    React frontend with:
    - Vite + TypeScript + TailwindCSS setup
    - TanStack Query for API state management
    - StockCard component for recommendation display
    - RadarChart component using Recharts
    - LogicSummary component for logic affiliation
    - MacroOverview component for macro dashboard
  </what-built>
  <how-to-verify>
    1. Start backend: cd /Users/heyecheng/Program/llm/stock_logic && uvicorn src.api.app:app --reload
    2. Start frontend: cd webui && npm run dev
    3. Visit: http://localhost:5173
    4. Verify:
       - Stock cards display with price, change %, marker badge
       - Radar charts render with 5 dimensions (Logic, Market, Macro, Sector, Sentiment)
       - Logic summaries show strength bars
       - Macro overview shows regime status and scores
  </how-to-verify>
  <resume-signal>Type "approved" if UI renders correctly, or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | Frontend makes HTTP requests to backend; input validation on both sides |
| User → Browser | User input (overrides, config edits) must be validated before submission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | XSS | React components | mitigate | React auto-escapes by default; never use dangerouslySetInnerHTML; CSP headers in production |
| T-06-02-02 | CSRF | Form submissions | mitigate | SameSite=strict cookies; backend CSRF token validation for mutations |
| T-06-02-03 | Information Disclosure | API responses | accept | Frontend displays only what backend provides; backend controls data visibility |
| T-06-02-04 | Tampering | Manual override forms | mitigate | Backend validates input ranges (0-1); audit trail logs all changes |
</threat_model>

<verification>
- Frontend builds: cd webui && npm run build succeeds without errors
- TypeScript compiles: No type errors in components or hooks
- Components render: All four components (StockCard, RadarChart, LogicSummary, MacroOverview) are exported and can be imported
- API integration: useRecommendations hook calls correct endpoint (/api/v1/recommendations)
</verification>

<success_criteria>
1. Vite + React 19 + TypeScript project initialized with correct dependencies
2. TanStack Query configured with 5-minute staleTime
3. Recharts radar charts render 5-dimension data correctly
4. StockCard displays price, change%, marker badge, position recommendation
5. LogicSummary shows logic details with visual strength bar
6. MacroOverview displays regime, quadrant, multiplier, and 5 dimension scores
7. TailwindCSS configured with stock-specific colors (up/down/neutral)
8. All components build without TypeScript errors
</success_criteria>

<output>
After completion, create .planning/phases/phase-6/06-02-SUMMARY.md
</output>
